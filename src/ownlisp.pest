program = {soi ~ (expression+)? ~ eoi}
sexpression = { "(" ~ expression* ~ ")" }
expression = { number | operator | sexpression }
number = @{"-"? ~ digit+}
operator = {"+" | "-" | "*" | "/" | "%" | "^" | "min" | "max" }
digit = _{'0'..'9'}

whitespace = _{" "}